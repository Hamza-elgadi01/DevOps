trigger:
  branches:
    include:
    - main

pool:
  vmImage: ubuntu-latest
  parallel: 2 
  
steps:
- task: NodeTool@0
  inputs:
    versionSpec: '14.x'
  displayName: 'Install Node.js'

- script: |
    npm ci --also=dev
  displayName: 'Install dependencies'
